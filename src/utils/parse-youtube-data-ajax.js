/**
 * @description parser for youtube data
 *      pages: `/`, `/watch`, `/results`
 *      sources: ajax (fetch)
 * @param {object} ajaxData json from our fetch interceptor
 */
export function parseYoutubeDataAjax (ajaxData) {
       
    // wip

}